name: Apply final Hafizku source once

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect complete staged source
        id: staged
        run: |
          count=$(find . -maxdepth 1 -type f -name 'final-source.part-*' | wc -l)
          if [ "$count" -eq 7 ]; then
            echo "ready=true" >> "$GITHUB_OUTPUT"
          else
            echo "ready=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Reassemble and apply staged source
        if: steps.staged.outputs.ready == 'true'
        run: |
          rm -f final-source.tar.gz
          cat final-source.part-* | base64 -d > final-source.tar.gz
          echo "f85e7bc833cd0ba7d315726ee37dacda130f33f7daf40c26f42c7545296ff036  final-source.tar.gz" | sha256sum -c -
          tar -xzf final-source.tar.gz
          rm -f final-source.tar.gz final-source.part-* audio-fix.js
          node --check app.js
          node --check data.js
          node --check sw.js

      - name: Commit final source
        if: steps.staged.outputs.ready == 'true'
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A -- ':!.github/workflows/pages.yml'
          git commit -m "Complete Hafizku source refactor [source-applied]"
          git push origin HEAD:main
